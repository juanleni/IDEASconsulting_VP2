# IDEAS / IDEUS — contexto del proyecto

Repo local: `C:\Users\RRHH\Documents\IDEAS`. GitHub: `juanleni/IDEASconsulting_VP2` (branch `main`).
Deploy: Render — servicio `ideas-consulting-v2` (plataforma principal) + `ideas-mobile-legal-matrix` (app mobile). Config en `render.yaml`, ambos `autoDeploy: false` (deploy manual desde el dashboard de Render).

Git no está en el PATH en esta máquina: usar `& "C:\Program Files\Git\cmd\git.exe" ...`.

## Qué es esto

Plataforma de consultoría. **IDEAS Consulting** es la consultora; **IDEUS** es el nombre del producto/plataforma en sí (sistema de gestión), mostrado como "IDEUS — by IDEAS Consulting". Sitio público institucional + sistema de gestión interno para clientes: Calidad/8D, Ambiente, SST, Riesgos, KPIs, Documentos, Laboratorio, Mapas de proceso y **Matriz Legal Digital** (el módulo con más desarrollo activo últimamente).

## Arquitectura — 3 frentes en paralelo

1. **`nicegui_v2/`** — plataforma principal en producción. NiceGUI + SQLite (`ideas.db` en la raíz del repo, resuelto por ruta relativa). Contiene la web pública (`pages_public.py`, `institutional_app.py`) y el sistema de gestión (`app.py` es el shell/router principal, `modules_*.py` un módulo por área, `core_data.py`/`database.py` acceso a datos). Archivos delicados: `app.py`, `pdf_reports.py`, `render.yaml`, `requirements.txt` — tocar con cuidado, guardar copia antes de cambios grandes.
2. **`mobile_legal_matrix/`** — PWA independiente (puerto 8600 local), un solo módulo (Matriz Legal). No depende de que `nicegui_v2` esté corriendo, pero **reutiliza sus helpers** (`modules_legal_matrix.py`, `core_data.py`, misma `ideas.db`, misma tabla `usuarios`, misma función `verificar_usuario`) — no duplica lógica de negocio. Login real, splash screen, carga de evidencia (cámara/galería/archivo). Deployada en Render sobre HTTPS real (necesario para service workers / push a futuro). Ver `mobile_legal_matrix/README.md` y `mobile_legal_matrix/SPEC_mobile_legal_matrix_v2.md` para el detalle de producto y el roadmap (Face ID/WebAuthn, diff de actualizaciones IDEAS, push notifications, KPIs nuevos — todo eso está spec'ado pero no implementado aún).
3. **`app/`** — prototipo WIP de una arquitectura nueva (FastAPI multi-tenant + Postgres, routers/servicios/repositorios separados). Empezado 2026-07-20 (commit `8eca987`, marcado WIP). Embrionario, no reemplaza nada en producción todavía. **En pausa, ver `ADR-001-arquitectura-app-prototipo.md`** (2026-08-10): recomendación es no seguir invirtiendo ahí por ahora (sin tests, no arranca tal cual en un entorno limpio, no conectado a Render, sin razón de negocio documentada para Postgres hoy) y en cambio replicar el patrón de "capa de servicios" directamente sobre `nicegui_v2/` — recomendación pendiente de confirmar por Juan, no una decisión ya tomada.

## Estado al 2026-08-11

**Commits del 2026-07-27** — foco: Matriz Legal Digital (mobile, scheduler de alertas, panel de curación normativa con SAIJ/Boletín Oficial/SRT Digesto).

**Commits del 2026-08-10 (primera tanda)** — rebranding a IDEUS, deep-link de admin por `empresa_id`, panel de contraseña/alertas de Matriz Legal en Usuarios, tests, prototipos de curación como referencia.

**Commits del 2026-08-10/11 (segunda tanda) — sprint de robustecimiento integral en 5 fases**, trabajado desde un Proyecto de Claude.ai ("Cowork") y commiteado localmente en esta sesión (quedó hecho pero sin commitear por un `.git/index.lock` stale de esa sesión anterior, ya limpiado):

- **Fase 0-1 — seguridad + aislamiento multi-tenant**: secrets movidos a env vars de Render; `ideas.db` sacado del tracking de git (ver más abajo); `core_data.py` con fixes de `empresa_id` en funciones de escritura que antes permitían tocar datos de otra empresa (mismo fix en `modules_lab.py`); rate limiting básico de login (5 intentos / 15 min → bloqueo 15 min); CI con GitHub Actions corriendo pytest en cada push/PR; base de backup automático diario (`db_backup_scheduler.py`) + bloque de disco persistente documentado y comentado en `render.yaml` (activar solo si se pasa a plan pago de Render).
- **Fase 2 — gaps funcionales QHSE**: 3 módulos nuevos — **Auditorías Internas** (`modules_audits.py`, ISO 9.2), **Revisión por la Dirección** (`modules_management_review.py`, ISO 9.3, con snapshot automático de auditorías/8D/riesgos/KPIs/legal), **Gestión Documental real** (`modules_document_control.py`, repositorio con versiones/estados, distinto de la biblioteca de referencia normativa existente); SST completado (incidentes, matriz de peligros, EPP); y se cerró el gap de curación normativa: ahora "aprobar" una norma deja elegir a qué empresa(s) se publica (`norma_empresa_publicacion`).
- **Fase 3 — marca y experiencia**: tipografía Poppins (self-hosted, `assets/fonts/`) de punta a punta — plataforma, sitio público, iframe de Matriz Legal, PDFs (con embebido de fuente verificado), app mobile; wordmark IDEUS extendido a Matriz Legal y mobile; bug corregido (menú lateral no se mostraba para `EMPRESA_ADMIN`/`EMPRESA_USER`); **exports de PDF (KPIs, mapa de procesos, 8D) movidos a `run.io_bound` con estado de loading en el botón** — antes corrían síncronos sobre el event loop y bloqueaban el websocket de todos los clientes conectados mientras duraba la generación.
- **Fase 4 — deuda técnica y arquitectura**: `requirements.txt` podado (streamlit/pyinstaller/fpdf2/PyMuPDF fuera, sin uso en producción, movidos a `requirements-legacy-portable.txt`); piloto de extracción de capa de servicios — `modules_legal_matrix.py` dividido en `legal_matrix_service.py` (datos/lógica, sin NiceGUI, testeable) + `modules_legal_matrix.py` (solo UI/rutas); **`ADR-001-arquitectura-app-prototipo.md`** (nuevo): recomienda pausar `app/` (ver sección de arquitectura arriba) — pendiente de confirmación de Juan, no una decisión tomada.

**Commits del 2026-08-21/24 — dos rondas de auditoría manual sobre la instancia Empresa Demo (10 hallazgos + 6 de seguimiento), todo cerrado:**

- **Ronda 1 (2026-08-21)**: los dos 500 bloqueantes (Usuarios: columna `delete_all_password_hash` sin migrar; Mapas de Proceso: mojibake en una clave de dict) resueltos y verificados con Playwright; manejador global de errores agregado (`app.exception_handler(Exception)` en `app.py`, pantalla de marca en vez de traza cruda); **RBAC real para Matriz Legal**: el selector "Administrador/Editor/Aprobador/Lector" era 100% cosmético (JS local, ningún endpoint lo verificaba) — ahora hay una tabla `legal_matrix_user_roles` y `_require_matrix_permission()` en `legal_matrix_service.py` hace cumplir el permiso en cada endpoint de escritura (crear/editar/borrar/importar/aprobar), con asignación de rol por usuario desde Usuarios; fila de "Módulos Operativos" migrada de `ui.tabs()` (Quasar, scroll con bugs de timing) a un contenedor con `overflow-x:auto` nativo; datos de relleno en Gestión Documental reemplazados; naming de pestaña/título unificado (Riesgos, SST).
- **Ronda 2 (2026-08-24, panel de cliente)**: confirmó los 7 fixes de la ronda 1 sin regresiones y agregó un test HTTP real (`TestMatrixRoleEndpoints` en `tests/test_legal_matrix.py`, via `fastapi.testclient.TestClient` contra las rutas reales) que prueba que un rol sin permiso recibe 403 del endpoint en vivo, no solo de la función de chequeo — era el único hallazgo de seguridad que seguía sin poder darse por cerrado. Se agregaron tooltips a los ~42 botones de solo-ícono del sistema (KPIs, Auditorías, SST, Calidad, Documentos, etc.) que no tenían `aria-label` ni `tooltip()`.
- **Ronda 3 (2026-08-24, consola Súper Admin `/dashboard`)**: **"Restaurar backup" ahora exige confirmación reforzada** — el nombre del archivo escrito a mano (patrón GitHub), fecha/tamaño mostrados en el diálogo, advertencia explícita de que el restore sobrescribe la base completa (un solo `ideas.db` para todos los tenants, no por-empresa), y queda registrado en `audit_log` (`obtener_ultima_restauracion_backup()` en `core_data.py`, mostrado debajo del control). **Bug real, no solo "a confirmar con producto"**: el mismo patrón de lockout de `530e656` (un `EMPRESA_ADMIN` bloqueado de un módulo por una fila vieja en `user_modules`) volvió a aparecer, esta vez con LAB ISO 17025 — el fix de esa vez solo cubrió el módulo `users` puntualmente; ahora `_can_access_module_by_assignment()` en `app.py` generaliza la regla: un `EMPRESA_ADMIN` siempre se rige por `company_modules` (lo que su empresa tiene habilitado), nunca por el toggle granular por-usuario — ese toggle solo debería restringir cuentas `EMPRESA_USER`. Las tarjetas de "Accesos estratégicos" del Dashboard General (mismo patrón `ui.tabs()`+`.on('click')` que causaba el bug de scroll de la ronda 1) pasaron a botones directos para eliminar la condición de carrera "el primer click resalta, el segundo navega a la tarjeta anterior". Curación Normativa (208 registros sin paginar) ahora pagina de a 20. "Modo interno" investigado: es un `ui.badge()` puramente informativo, nunca fue clickeable ni está atado a ningún flag — no requería fix.

**Commit del 2026-08-24 — diagnóstico inicial parcial/con alcance** (`pages_diagnostic.py`): pedido de Juan, no hallazgo de auditoría. Antes `/diagnostico` siempre forzaba los 14 ejes/86 preguntas completos; ahora hay un panel "Alcance del diagnóstico" con checkboxes por eje (`EJES_SIEMPRE` — Dirección y Estrategia, Mapa de Procesos, Organización, Cultura y Madurez — bloqueados siempre-on) y presets de un click por línea de servicio (Ambiental y SST, Calidad, Operaciones, Comercial y Finanzas, o completo), hardcodeados en `pages_diagnostic.py` por ahora. No hizo falta tocar el motor de scoring (`build_eje_scores`/`build_plan`/radar/PDF): ya calculaba todo dinámicamente a partir de los ejes con respuestas guardadas, sin asumir el universo de 14. El alcance de un corte se muestra en Resultados/Historial/PDF ejecutivo leyendo directamente qué ejes tienen respuesta — sin columna nueva en la base.

**Commit del 2026-08-25 — rediseño de `/diagnostico`**: pedido de Juan ("quedó antiguo, no amigable para completar"), no hallazgo de auditoría. Mismo lenguaje visual que el resto de IDEUS (navy/teal/verde, Poppins, cards con blur). El `<select>` de puntaje 1-4 pasó a ser 4 pills clickeables con color según selección (rojo/naranja/azul/verde) y tooltip con la descripción del criterio; la evidencia pasó de N inputs dinámicos a un solo textarea multilínea (`split_evidence_values` ya parseaba comas/saltos de línea, así que no cambió el formato guardado); los checkboxes de alcance pasaron a chips toggle; nuevo banner "Avance del relevamiento" con barra de progreso en base a preguntas *con evidencia cargada* (no "respondidas" — todas arrancan en 3 por default, así que eso no sería progreso real); cada eje muestra un badge de score promedio en vivo en el header del acordeón. `save_diagnosis()` ahora lee de `answers_state` en vez de iterar widgets vivos.

**Commit del 2026-08-25 (mismo día, segunda vuelta) — panel de alcance agrupado + evidencia con archivos/fotos**: la nube de chips de la primera vuelta del rediseño quedó mal ("es un espanto") — ahora es un panel de gestión: tira de solo-lectura para el núcleo siempre-incluido, más un grid de cards (uno por línea de servicio de `EJE_PRESETS`) con switches por eje. Evidencia ahora acepta archivos/fotos de verdad, no solo texto: `ui.upload` por pregunta (imagen/PDF/Office; en mobile el picker nativo ofrece cámara sin restringir a solo-camara), tabla nueva `diagnostico_evidencia_archivos` (gitignored bajo `assets/diagnostico_evidencia/`, mismo criterio que `ideas.db`), con ciclo de vida "borrador" (`diagnostico_id NULL`, scopeado por empresa/eje/pregunta) → "reclamado" al guardar (`asignar_evidencia_archivos`) o descartado al cancelar (`descartar_evidencia_borrador`). El selector de empresa ahora gatea el resto del formulario (`rest_of_form` refreshable) porque la ruta de guardado de archivos necesita saber la empresa de antemano. **Bug real encontrado y corregido en el camino**: `uploader.reset()` (para no duplicar el preview propio de NiceGUI con el custom) generaba un 404 interno que rompía la sesión en memoria de la pagina lo suficiente como para que el archivo subido no quedara "reclamado" al guardar — se sacó esa llamada, prioridad a integridad de datos sobre prolijidad visual del duplicado.

**Commit del 2026-08-26 — mobile real para el módulo de diagnóstico** (`/diagnostico` + `/resultados` + `/historial`, shell compartido): pedido de Juan, probado con Playwright emulando un iPhone (375×812, touch, UA mobile) chequeando `scrollWidth` vs `clientWidth` en cada paso, no solo capturas. Tres bugs de fondo encontrados y corregidos (no específicos de diagnóstico, afectan cualquier página que use estos patrones):
- **El drawer lateral arrancaba abierto y tapaba toda la pantalla en celular** hasta que alguien lo cerraba a mano una vez. `_is_mobile_request()` (nuevo helper en `app.py`, sniff de User-Agent — no hay forma de leer el viewport real en el render inicial del lado del servidor) ahora pisa el default a cerrado *solo* la primera vez que alguien entra sin preferencia guardada; en cuanto se toca el toggle, `sidebar_open` en storage manda siempre, sin importar el dispositivo.
- **La fila de acciones de la topbar** (Smart IDEAS / AI Command Center / Web institucional / Salir) se envolvía en 3-4 líneas en celular, comiéndose como un tercio de la pantalla. Un intento con media query CSS (ocultar el label, dejar el ícono) no funcionó pese a que la regla estaba bien armada y con `!important` — no se pudo aislar por qué el navegador no la aplicaba. Se resolvió del lado del servidor con el mismo `_is_mobile_request()`: botones sin texto (con tooltip) directamente en Python.
- **`ui.grid(columns=N)` no colapsaba a 1 columna en celular** pese a que las reglas sitewide para `.ideas-grid-2`/`.ideas-grid-3` en 1100px/760px ya existían — NiceGUI pone `grid-template-columns` como estilo inline, que le gana a una clase sin `!important`. Se vio en `/resultados`: el radar "Balance de Madurez por Eje" quedaba a media pantalla, ilegible. Se agregó `!important` a esas dos reglas (arregla esto en cualquier otro lado del sitio que use el mismo patrón, no solo ahí).

Además, específico de `/diagnostico`: los pills de puntaje pasan de 4 en fila a grid 2×2 bajo 700px; el textarea de evidencia y el de observación (que se repartían el ancho a la mitad vía `.col` de Quasar, inusable en celular) pasan a apilarse (`.ideas-stack-row`); el header de cada pregunta ya no fuerza `no-wrap`. **Carga de evidencia con cámara y galería como dos botones separados** (no uno solo): "Tomar foto" (`accept="image/*" capture="environment"`, abre la cámara trasera directo en mobile) y "Galería / archivo" (sin `capture`, acepta también PDF/Office) — en desktop `capture` no hace nada, así que ambos abren el selector de archivos de siempre.

Detalle completo, fecha por fecha, en `CHANGELOG_STATUS.txt`.

**Investigado en esta sesión — "connection lost" intermitente en local:** la causa más probable eran justamente los exports de PDF sync mencionados en Fase 3 (ya corregidos ahí). Quedan **sin corregir** otros puntos con el mismo patrón — llamadas bloqueantes directo en el loop de un scheduler async, sin `asyncio.to_thread`/`run.io_bound` — que también pueden gatillarlo: `services/lab_ai_scheduler.py` (llamadas a OpenAI), `services/legal_matrix_alert_scheduler.py` (envío de email SMTP), `services/db_backup_scheduler.py` (copia del archivo de base). `services/legal_curation_scheduler.py` ya está bien resuelto (usa `asyncio.to_thread` a propósito, con comentario explicando por qué). Si el problema persiste después de esta tanda, ahí es donde mirar primero.

## Pendientes conocidos

- Pushear todos estos commits a GitHub y hacer Manual Deploy en Render (`autoDeploy` está en `false`).
- `nicegui_v2/ideas.db` — archivo suelto sin trackear que no debería existir ahí (la app espera `ideas.db` solo en la raíz). Sin confirmar aún si borrarlo.
- Confirmar o corregir la recomendación de `ADR-001` sobre `app/`.
- Decidir si se sube el plan de Render a uno pago para activar disco persistente + backup real.
- Borrar logos demo con marcas de terceros (Pepsi, Adidas, Coca-Cola) y PDFs de prueba generados durante la verificación de Fase 4 (permisos no dejaron borrarlos desde esa sesión).
- Replicar el patrón de extracción de capa de servicios (piloto en Matriz Legal, Fase 4) en el resto de los módulos.
- Wrappear en `asyncio.to_thread`/`run.io_bound` las llamadas bloqueantes que quedan en `lab_ai_scheduler.py`, `legal_matrix_alert_scheduler.py` y `db_backup_scheduler.py` (ver nota de "connection lost" arriba).
- Definir el mapeo empresa↔norma quedó resuelto en Fase 2 — lo que sigue abierto ahí es el resto del roadmap del panel de curación (nada bloqueante conocido).
- Calidad/8D: editar y eliminar acciones D5/D6 individuales desde la tabla, múltiples árboles de 5 Porqués por factor retenido, llevar acciones D5/D6 al PDF ejecutivo.
- Mobile (`SPEC_mobile_legal_matrix_v2.md`): Face ID/WebAuthn, diff de actualizaciones de IDEAS, push notifications, KPIs nuevos del dashboard.
- **Alcance del diagnóstico** (`EJES_SIEMPRE`/`EJE_PRESETS` en `pages_diagnostic.py`): hardcodeado por decisión de Juan. Si el catálogo de servicios/presets empieza a cambiar seguido, migrarlo a una hoja nueva de `Data/diagnostico.xlsx` (mismo patrón que preguntas y criterios de puntuación) para que se pueda editar sin tocar código.
- **Unificar patrón de navegación entre módulos** (hallazgo #4 de la auditoría, no urgente): Documentos y KPIs expanden contenido inline debajo de la fila de pestañas; el resto navega a una URL nueva completa. El botón "Volver al sistema de gestión" solo existe en algunas pantallas. Falta decidir un único patrón y aplicarlo a las ~11 pantallas.
- **Antes de exponer esto en red** (staging, demo a cliente, producción — no aplica mientras se corra en local `http://127.0.0.1:8502`): forzar HTTPS + HSTS, confirmar `Secure`/`HttpOnly`/`SameSite` en la cookie de sesión, y revisar qué eventos manda la cookie de analítica de Segment (`ajs_anonymous_id`, sin aviso de cookies visible) dado que el sistema maneja datos sensibles (accidentes laborales, auditorías, no conformidades) — evaluar si corresponde un aviso de privacidad.
- **Política de retención de la memoria del copiloto (consola Súper Admin → "Memoria por empresa")**: decisión pendiente de Juan, no de código. Definir por escrito cuánto tiempo se retiene el historial de consultas de cada usuario a Smart IDEAS, quién dentro del equipo IDEAS puede accederlo, y reflejarlo en el acuerdo con cada cliente. Los controles de borrado ("Limpiar y conservar", "Limpiar todo") ya existen; si se quiere, sumar un log de "quién abrió la memoria de qué empresa y cuándo" además de eso.
- **Doble aprobación para restaurar un backup**: la confirmación reforzada (nombre de archivo + advertencia + log) ya está — evaluar si además conviene exigir que una segunda persona apruebe el restore, dado que afecta a toda la plataforma de una vez. No implementado — es una decisión de proceso, no solo de UI.

## Accesos y flujo de trabajo

- Login plataforma: usuario `IDEAS`, contraseña `2026`, ruta `/plataforma`.
- **Intérprete correcto para correr local: Python 3.10**, no el `python` por default del PATH. Esta máquina tiene varios Python instalados (3.14 en `C:\Python314`, 3.10 en `C:\Users\RRHH\AppData\Local\Programs\Python\Python310`) y solo el 3.10 tiene NiceGUI y el resto de las dependencias instaladas. Usar la ruta completa: `"C:\Users\RRHH\AppData\Local\Programs\Python\Python310\python.exe" nicegui_v2/app.py` (plataforma, puerto 8502) / `... mobile_legal_matrix/app.py` (mobile, puerto 8600) — correr desde la raíz del repo, con `/` en la ruta (no `\`, si se ejecuta desde Git Bash una barra invertida antes de una letra se come el separador).
- Flujo: editar local → probar local → commit → push a GitHub → si Render no auto-actualiza (`autoDeploy: false`), Manual Deploy → "Deploy latest commit" desde el dashboard.
- Detalle completo de deploy/troubleshooting en `ACTUALIZACION_WEB_IDEAS.txt` (plataforma principal) y `mobile_legal_matrix/DEPLOY_RENDER.md` (mobile).
- Changelog ejecutivo (para uso no técnico) en `CHANGELOG_STATUS.txt` — mantenerlo actualizado con fecha / módulo / qué cambió / impacto / pendientes cuando se cierre trabajo grande.
- Si `git` tira `Unable to create '.git/index.lock': File exists` y no hay ningún proceso `git.exe` corriendo (`tasklist`), es un lock stale de una sesión anterior — borrar `.git/index.lock` a mano y reintentar.
